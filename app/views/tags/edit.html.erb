<h1>Edit Tag</h1>

<%= form_for [@product, @tag] do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Update Tag" %>
<% end %>

<%= link_to "Back", product_tags_path(@product) %>